program input;
var a, nigga, pens, _$$$44 : integer ;
var b : integer ;
var temp : integer ;
var c : boolean;
begin
	a := 1 ;
	b := 7 + -a;
	c := true;
	while True do 
		begin
			writeln (1);
			temp := b ;
			b := 2 + temp;;;;;;
			b := a - 2;
			b := a * b;
			b := a div 2;
			b := (a + b) div 4; 
				begin
				end;
			b := a mod 2;
			b := a mod 3;
			writeln(2);
			if true then 
				begin
					c := true;
				end
			else 
				writeln(a);

			writeln(3);

			if not c then 
			if c then 
				begin 
				end 
			else 
				begin 
					;;; 
				end;

			writeln(4);

			c := false;
	
			while true do 
				begin
					a := a + 1;
					break;
				end;
			
			writeln(5);

			break;
			a := temp ; // Ich scheisse auf Leichen..		;;;;
		end
	{Du kackmongo...


	sdmsd		...	.;
	sdss...}
end.